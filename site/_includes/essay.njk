
---
layout: base.njk
---
<article class="card">
  <h1>{{ title }}</h1>
  <div class="meta">
    <span class="badge">Version {{ version }}</span>
    {% if initial_status == "complete" %}
      <span class="badge">Published completed</span>
    {% else %}
      <span class="badge">Published unfinished</span>
    {% endif %}
    <span class="badge">{{ word_range }} words</span>
    {% if author %}<span>By <strong>@{{ author }}</strong></span>{% endif %}
    {% if coauthors and coauthors.length %}<span>with {% for a in coauthors %}@{{a}}{% if not loop.last %}, {% endif %}{% endfor %}</span>{% endif %}
    {% if published_at %}<span>Published: {{ published_at | date("yyyy-LL-dd") }}</span>{% endif %}
  </div>
  <div class="content">
    {{ content | safe }}
  </div>
</article>

<section class="card">
  <h3>Release notes</h3>
  {% if release_notes and release_notes.length %}
    <ul>
      {% for note in release_notes %}
        <li>{{ note }}</li>
      {% endfor %}
    </ul>
  {% else %}
    <p>No release notes yet.</p>
  {% endif %}
</section>

<section class="card">
  <h3>Acknowledgments</h3>
  {% if acknowledgments and acknowledgments.length %}
    <ul>
      {% for ack in acknowledgments %}
        <li>@{{ ack.user }} — {{ ack.note }} (since v{{ ack.since_version }})</li>
      {% endfor %}
    </ul>
  {% else %}
    <p>Contribute a minor improvement (grammar, phrasing, examples, references) to be credited here.</p>
  {% endif %}
</section>
